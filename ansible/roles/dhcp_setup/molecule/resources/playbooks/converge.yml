---
#
- name: Converge
  hosts: all
  gather_facts: true
  become: true
  vars:
    role_name: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') | basename }}"

  tasks:
    - name: Play the role
      ansible.builtin.include_role:
        name: "{{ role_name }}"
...
