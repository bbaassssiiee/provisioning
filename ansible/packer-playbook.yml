#!/usr/bin/env ansible-playbook
---

- name: Hardening
  hosts: all
  become: true
  gather_facts: true

  vars:
    rhel8cis_rule_5_3_4: false
    crypto_policy: DEFAULT

  roles:
    - RHEL8-CIS
    - openssh
    - base_line

...
